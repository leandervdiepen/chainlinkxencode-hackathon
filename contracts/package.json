{
  "name": "contracts",
  "version": "0.0.1",
  "description": "The web3 solidity smart contracts needed ready compiled for usage with the next-chainlink package.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  }
}
